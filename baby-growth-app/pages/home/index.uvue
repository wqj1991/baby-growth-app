<template>
  <uni-tab-bar class="page-root" :active-index="activeIndex">
    <view class="page-content-shell">
      <uni-nav-bar
        title=""
        :hide-default-back="true"
        navigation-bar-text-style="#1f2937"
        left-class="home-nav-left"
        mid-class="home-nav-mid"
        right-class="home-nav-right"
      >
        <template #mid>
          <view class="nav-title-block">
            <text class="nav-title">宝宝的动态</text>
            <text class="nav-subtitle">1岁8个月14天 · 今日同步正常</text>
          </view>
        </template>
        <template #right>
          <view class="nav-actions">
            <view class="nav-icon-button" @tap="showToast('搜索功能后续接入')">
              <uni-icons type="search" :size="18" color="#344054"></uni-icons>
            </view>
            <view class="nav-icon-button nav-icon-button-primary" @tap="showToast('拍照入口后续接入')">
              <uni-icons type="camera-filled" :size="18" color="#ffffff"></uni-icons>
            </view>
          </view>
        </template>
      </uni-nav-bar>

      <scroll-view class="page-scroll" :show-scrollbar="false">
        <view class="hero-section">
          <view class="summary-card">
            <view class="summary-copy">
              <view class="summary-chip">
                <uni-icons type="calendar" :size="14" color="#dce9f4"></uni-icons>
                <text class="summary-chip-text">今日成长摘要</text>
              </view>
              <text class="summary-title">喂养、睡眠与影像集中展示</text>
              <text class="summary-desc">首页先完成浏览和快速入口，后续逐步接入同步、记录和详情跳转。</text>
            </view>
            <view class="summary-badge">
              <text class="summary-badge-number">24</text>
              <text class="summary-badge-label">小时动态</text>
            </view>
          </view>

          <view class="stats-grid">
            <view v-for="(item, index) in statCards" :key="index" class="stat-card" :style="{ backgroundColor: item.bgColor }">
              <view class="stat-head">
                <view class="stat-icon-box" :style="{ backgroundColor: item.iconBg }">
                  <uni-icons :type="item.icon" :size="17" :color="item.titleColor"></uni-icons>
                </view>
                <text class="stat-title" :style="{ color: item.titleColor }">{{ item.title }}</text>
              </view>
              <text class="stat-value">{{ item.value }}</text>
              <text class="stat-footnote">{{ item.footnote }}</text>
            </view>
          </view>

          <view class="quick-actions">
            <view class="quick-action quick-action-primary" @tap="showToast('快速拍照后续接入')">
              <view class="quick-action-head">
                <uni-icons type="camera-filled" :size="20" color="#d65f3d"></uni-icons>
                <text class="quick-action-title">快速拍照</text>
              </view>
              <text class="quick-action-desc">记录宝宝当下瞬间</text>
            </view>
            <view class="quick-action quick-action-secondary" @tap="showToast('快速打卡后续接入')">
              <view class="quick-action-head">
                <uni-icons type="compose" :size="20" color="#2d6fd2"></uni-icons>
                <text class="quick-action-title">快速打卡</text>
              </view>
              <text class="quick-action-desc">喂养 睡眠 大小便</text>
            </view>
          </view>
        </view>

        <view class="timeline-section">
          <view class="section-header">
            <view>
              <text class="section-title">全家实时动态</text>
              <text class="section-subtitle">照片、视频和护理记录统一收口</text>
            </view>
            <view class="section-link-box" @tap="showToast('时间线列表后续扩展')">
              <text class="section-link">查看全部</text>
              <uni-icons type="right" :size="12" color="#5b7cf2"></uni-icons>
            </view>
          </view>

          <view v-for="(item, index) in timelineItems" :key="index" class="timeline-card">
            <view class="timeline-head">
              <view class="avatar" :style="{ backgroundColor: item.avatarColor }">
                <text class="avatar-text">{{ item.avatarText }}</text>
              </view>
              <view class="author-meta">
                <view class="author-row">
                  <text class="author-name">{{ item.author }}</text>
                  <view class="type-tag" :style="{ backgroundColor: item.tagBg, borderColor: item.tagBorder }">
                    <text class="type-tag-text" :style="{ color: item.tagTextColor }">{{ item.tagText }}</text>
                  </view>
                </view>
                <text class="author-time">{{ item.time }}</text>
              </view>
            </view>

            <text class="timeline-copy">{{ item.content }}</text>

            <view v-if="item.kind != 'log'" class="media-preview" :style="{ backgroundColor: item.previewBg }">
              <image v-if="item.imageSrc.length > 0" class="media-image" :src="item.imageSrc" mode="aspectFill"></image>
              <view v-if="item.kind == 'video'" class="play-badge">
                <uni-icons type="videocam-filled" :size="16" color="#ffffff"></uni-icons>
                <text class="play-badge-text">视频预览</text>
              </view>
              <view class="preview-overlay">
                <text class="preview-title">{{ item.previewTitle }}</text>
                <text class="preview-subtitle">默认低清缓存预览，原文件仅在用户主动下载时保存本地。</text>
              </view>
            </view>

            <view v-else class="log-panel">
              <view class="log-icon-box">
                <uni-icons :type="item.logIcon" :size="20" color="#ffffff"></uni-icons>
              </view>
              <view class="log-copy">
                <text class="log-title">{{ item.previewTitle }}</text>
                <text class="log-desc">{{ item.logDetail }}</text>
              </view>
            </view>

            <view class="timeline-actions">
              <view class="action-group">
                <view class="metric-chip">
                  <uni-icons type="heart" :size="14" color="#98a2b3"></uni-icons>
                  <text class="metric-text">{{ item.likes }}</text>
                </view>
                <view class="metric-chip">
                  <uni-icons type="chat" :size="14" color="#98a2b3"></uni-icons>
                  <text class="metric-text">{{ item.comments }}</text>
                </view>
              </view>
              <view class="action-buttons">
                <view class="action-button secondary-button" @tap="showToast('缓存预览功能后续接入')">
                  <uni-icons type="eye" :size="14" color="#475467"></uni-icons>
                  <text class="secondary-button-text">预览</text>
                </view>
                <view class="action-button primary-button" @tap="showToast('下载功能后续接入')">
                  <uni-icons type="download" :size="14" color="#ffffff"></uni-icons>
                  <text class="primary-button-text">下载</text>
                </view>
              </view>
            </view>
          </view>
        </view>

        <view class="page-bottom-space"></view>
      </scroll-view>
    </view>

    <tab-list class="home-tab-list">
      <tab-list-item>
        <view class="tab-item" @tap="showToast('当前位于首页')">
          <uni-icons type="home-filled" :size="22" color="#ff8a65"></uni-icons>
          <text class="tab-label tab-label-active">首页</text>
        </view>
      </tab-list-item>
      <tab-list-item>
        <view class="tab-item" @tap="showToast('快速记录页后续开发')">
          <uni-icons type="compose" :size="22" color="#98a2b3"></uni-icons>
          <text class="tab-label">记录</text>
        </view>
      </tab-list-item>
      <tab-list-item>
        <view class="tab-item tab-item-center" @tap="showToast('加号快捷入口后续接入')">
          <view class="tab-center-button">
            <uni-icons type="plusempty" :size="24" color="#ffffff"></uni-icons>
          </view>
        </view>
      </tab-list-item>
      <tab-list-item>
        <view class="tab-item" @tap="showToast('统计页后续开发')">
          <uni-icons type="bars" :size="22" color="#98a2b3"></uni-icons>
          <text class="tab-label">统计</text>
        </view>
      </tab-list-item>
      <tab-list-item>
        <view class="tab-item" @tap="showToast('我的页后续开发')">
          <uni-icons type="person-filled" :size="22" color="#98a2b3"></uni-icons>
          <text class="tab-label">我的</text>
        </view>
      </tab-list-item>
    </tab-list>
  </uni-tab-bar>
</template>

<script setup lang="uts">
  import tabList from '@/uni_modules/uni-tab-bar/components/uni-tab-bar/tab-list.uvue'
  import tabListItem from '@/uni_modules/uni-tab-bar/components/uni-tab-bar/tab-list-item.uvue'

  type StatCard = {
    title: string,
    value: string,
    footnote: string,
    icon: string,
    bgColor: string,
    iconBg: string,
    titleColor: string
  }

  type TimelineItem = {
    author: string,
    time: string,
    avatarText: string,
    avatarColor: string,
    tagText: string,
    tagBg: string,
    tagBorder: string,
    tagTextColor: string,
    content: string,
    kind: string,
    imageSrc: string,
    previewTitle: string,
    previewBg: string,
    likes: number,
    comments: number,
    logIcon: string,
    logDetail: string
  }

  const activeIndex = ref<number>(0)

  const statCards = ref<StatCard[]>([
    {
      title: '喂养',
      value: '5次',
      footnote: '奶粉 720ml',
      icon: 'wallet-filled',
      bgColor: '#EAF4FF',
      iconBg: '#D8EAFE',
      titleColor: '#246BCE'
    },
    {
      title: '睡眠',
      value: '10.5h',
      footnote: '午睡 1次',
      icon: 'moon-filled',
      bgColor: '#EAFBF3',
      iconBg: '#D9F7E6',
      titleColor: '#1E8E5A'
    },
    {
      title: '影像',
      value: '8条',
      footnote: '照片 6 视频 2',
      icon: 'image-filled',
      bgColor: '#FFF6E8',
      iconBg: '#FFE7C3',
      titleColor: '#B76A12'
    },
    {
      title: '换片',
      value: '6次',
      footnote: '状态稳定',
      icon: 'checkbox-filled',
      bgColor: '#F8EEFF',
      iconBg: '#EAD7FF',
      titleColor: '#7A44C2'
    }
  ])

  const timelineItems = ref<TimelineItem[]>([
    {
      author: '妈妈',
      time: '2小时前',
      avatarText: '妈',
      avatarColor: '#FFB9C9',
      tagText: '照片',
      tagBg: '#FFF1F4',
      tagBorder: '#FFD4DD',
      tagTextColor: '#C34E6B',
      content: '今天天气很好，带宝宝去楼下晒太阳，抓拍到一个特别开心的笑脸。',
      kind: 'photo',
      imageSrc: '/static/shuijiao.jpg',
      previewTitle: '公园散步抓拍',
      previewBg: '#FFD9D0',
      likes: 12,
      comments: 3,
      logIcon: '',
      logDetail: ''
    },
    {
      author: '爸爸',
      time: '5小时前',
      avatarText: '爸',
      avatarColor: '#A9D3FF',
      tagText: '视频',
      tagBg: '#EEF5FF',
      tagBorder: '#D4E6FF',
      tagTextColor: '#2D6FD2',
      content: '宝宝今天自己扶着沙发走了好几步，先在首页里保留低清预览和下载入口。',
      kind: 'video',
      imageSrc: '/static/shuijiao.jpg',
      previewTitle: '第一次扶走记录',
      previewBg: '#C9E2FF',
      likes: 25,
      comments: 8,
      logIcon: '',
      logDetail: ''
    },
    {
      author: '妈妈',
      time: '8小时前',
      avatarText: '妈',
      avatarColor: '#FFB9C9',
      tagText: '记录',
      tagBg: '#F4F0FF',
      tagBorder: '#E2D8FF',
      tagTextColor: '#6D54D8',
      content: '完成一条喂养记录，首页需要先支持摘要展示，后续再接完整记录详情跳转。',
      kind: 'log',
      imageSrc: '',
      previewTitle: '喂奶记录',
      previewBg: '#F3EEFF',
      likes: 4,
      comments: 1,
      logIcon: 'wallet-filled',
      logDetail: '奶粉 180ml · 08:30'
    }
  ])

  function showToast(title: string) {
    uni.showToast({
      title,
      icon: 'none'
    })
  }
</script>

<style>
  .page-root {
    flex: 1;
    height: 100%;
    background-color: #f6f8fc;
  }

  .page-content-shell {
    width: 100%;
    height: 100%;
    position: absolute;
  }

  .page-scroll {
    flex: 1;
    height: 100%;
  }

  .home-nav-left {
    width: 12px;
    margin-left: 0;
  }

  .home-nav-mid {
    left: 16px;
    right: 122px;
    height: 56px;
  }

  .home-nav-right {
    width: 116px;
    margin-right: 16px;
    justify-content: flex-end;
  }

  .nav-title-block {
    justify-content: center;
  }

  .nav-title {
    font-size: 26px;
    font-weight: 700;
    color: #1f2937;
  }

  .nav-subtitle {
    margin-top: 6px;
    font-size: 12px;
    color: #667085;
  }

  .nav-actions {
    flex-direction: row;
    align-items: center;
  }

  .nav-icon-button {
    width: 38px;
    height: 38px;
    border-radius: 19px;
    background-color: #ffffff;
    justify-content: center;
    align-items: center;
    margin-left: 10px;
  }

  .nav-icon-button-primary {
    background-color: #ff8a65;
  }

  .hero-section {
    padding: 14px 16px 0;
  }

  .summary-card {
    padding: 18px;
    border-radius: 24px;
    background-color: #1f4769;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    box-shadow: 0 12px 30px rgba(31, 71, 105, 0.18);
  }

  .summary-copy {
    flex: 1;
    padding-right: 12px;
  }

  .summary-chip {
    flex-direction: row;
    align-items: center;
  }

  .summary-chip-text {
    margin-left: 6px;
    font-size: 12px;
    color: #dce9f4;
  }

  .summary-title {
    margin-top: 10px;
    font-size: 18px;
    font-weight: 700;
    line-height: 26px;
    color: #ffffff;
  }

  .summary-desc {
    margin-top: 8px;
    font-size: 13px;
    line-height: 20px;
    color: #d8e7f5;
  }

  .summary-badge {
    width: 82px;
    height: 82px;
    border-radius: 24px;
    background-color: rgba(255, 255, 255, 0.18);
    justify-content: center;
    align-items: center;
  }

  .summary-badge-number {
    font-size: 26px;
    font-weight: 700;
    color: #ffffff;
  }

  .summary-badge-label {
    margin-top: 4px;
    font-size: 11px;
    color: #e5eef7;
  }

  .stats-grid {
    margin-top: 16px;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-between;
  }

  .stat-card {
    width: 48.4%;
    min-height: 118px;
    border-radius: 22px;
    padding: 16px;
    margin-bottom: 12px;
  }

  .stat-head {
    flex-direction: row;
    align-items: center;
  }

  .stat-icon-box {
    width: 32px;
    height: 32px;
    border-radius: 16px;
    justify-content: center;
    align-items: center;
  }

  .stat-title {
    margin-left: 10px;
    font-size: 14px;
    font-weight: 600;
  }

  .stat-value {
    margin-top: 14px;
    font-size: 24px;
    font-weight: 700;
    color: #101828;
  }

  .stat-footnote {
    margin-top: 6px;
    font-size: 12px;
    color: #667085;
  }

  .quick-actions {
    margin-top: 4px;
    margin-bottom: 8px;
    flex-direction: row;
    justify-content: space-between;
  }

  .quick-action {
    width: 48.4%;
    border-radius: 22px;
    padding: 18px 16px;
  }

  .quick-action-primary {
    background-color: #ffefe8;
  }

  .quick-action-secondary {
    background-color: #e9f4ff;
  }

  .quick-action-head {
    flex-direction: row;
    align-items: center;
  }

  .quick-action-title {
    margin-left: 8px;
    font-size: 16px;
    font-weight: 700;
    color: #1f2937;
  }

  .quick-action-desc {
    margin-top: 8px;
    font-size: 12px;
    color: #667085;
  }

  .timeline-section {
    padding: 18px 16px 0;
  }

  .section-header {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 14px;
  }

  .section-title {
    font-size: 20px;
    font-weight: 700;
    color: #111827;
  }

  .section-subtitle {
    margin-top: 4px;
    font-size: 12px;
    color: #98a2b3;
  }

  .section-link-box {
    flex-direction: row;
    align-items: center;
  }

  .section-link {
    font-size: 13px;
    color: #5b7cf2;
    font-weight: 600;
    margin-right: 4px;
  }

  .timeline-card {
    background-color: #ffffff;
    border-radius: 24px;
    padding: 16px;
    margin-bottom: 16px;
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.06);
  }

  .timeline-head {
    flex-direction: row;
    align-items: center;
  }

  .avatar {
    width: 42px;
    height: 42px;
    border-radius: 21px;
    justify-content: center;
    align-items: center;
  }

  .avatar-text {
    font-size: 16px;
    font-weight: 700;
    color: #ffffff;
  }

  .author-meta {
    flex: 1;
    margin-left: 12px;
  }

  .author-row {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }

  .author-name {
    font-size: 15px;
    font-weight: 700;
    color: #1f2937;
  }

  .author-time {
    margin-top: 4px;
    font-size: 12px;
    color: #98a2b3;
  }

  .type-tag {
    height: 28px;
    padding: 0 12px;
    border-radius: 14px;
    justify-content: center;
    align-items: center;
    border-width: 1px;
    border-style: solid;
  }

  .type-tag-text {
    font-size: 12px;
    font-weight: 600;
  }

  .timeline-copy {
    margin-top: 14px;
    font-size: 14px;
    line-height: 22px;
    color: #344054;
  }

  .media-preview {
    margin-top: 14px;
    height: 220px;
    border-radius: 22px;
    overflow: hidden;
    position: relative;
  }

  .media-image {
    width: 100%;
    height: 100%;
  }

  .play-badge {
    position: absolute;
    top: 16px;
    right: 16px;
    height: 34px;
    padding: 0 12px;
    border-radius: 17px;
    background-color: rgba(17, 24, 39, 0.5);
    flex-direction: row;
    align-items: center;
    justify-content: center;
  }

  .play-badge-text {
    margin-left: 6px;
    font-size: 11px;
    color: #ffffff;
    font-weight: 700;
  }

  .preview-overlay {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    padding: 16px;
    background-color: rgba(17, 24, 39, 0.34);
  }

  .preview-title {
    font-size: 16px;
    font-weight: 700;
    color: #ffffff;
  }

  .preview-subtitle {
    margin-top: 6px;
    font-size: 12px;
    line-height: 18px;
    color: #f3f4f6;
  }

  .log-panel {
    margin-top: 14px;
    border-radius: 20px;
    background-color: #f5f3ff;
    padding: 14px;
    flex-direction: row;
    align-items: center;
  }

  .log-icon-box {
    width: 48px;
    height: 48px;
    border-radius: 16px;
    background-color: #6d54d8;
    justify-content: center;
    align-items: center;
  }

  .log-copy {
    margin-left: 12px;
    flex: 1;
  }

  .log-title {
    font-size: 15px;
    font-weight: 700;
    color: #2d1b69;
  }

  .log-desc {
    margin-top: 5px;
    font-size: 12px;
    color: #6b5ca5;
  }

  .timeline-actions {
    margin-top: 16px;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }

  .action-group {
    flex-direction: row;
    align-items: center;
  }

  .metric-chip {
    flex-direction: row;
    align-items: center;
    margin-right: 14px;
  }

  .metric-text {
    margin-left: 4px;
    font-size: 12px;
    color: #667085;
  }

  .action-buttons {
    flex-direction: row;
    align-items: center;
  }

  .action-button {
    min-width: 72px;
    height: 34px;
    padding: 0 12px;
    border-radius: 17px;
    justify-content: center;
    align-items: center;
    flex-direction: row;
    margin-left: 8px;
  }

  .secondary-button {
    background-color: #f4f6fb;
  }

  .primary-button {
    background-color: #ff8a65;
  }

  .secondary-button-text {
    margin-left: 4px;
    font-size: 12px;
    color: #475467;
    font-weight: 600;
  }

  .primary-button-text {
    margin-left: 4px;
    font-size: 12px;
    color: #ffffff;
    font-weight: 600;
  }

  .page-bottom-space {
    height: 128px;
  }

  .home-tab-list {
    height: 78px;
    padding-top: 8px;
    background-color: rgba(255, 255, 255, 0.98);
    border-top-color: #e5e7eb;
  }

  .tab-item {
    align-items: center;
    justify-content: center;
    min-width: 54px;
  }

  .tab-item-center {
    margin-top: -28px;
  }

  .tab-label {
    margin-top: 6px;
    font-size: 11px;
    color: #98a2b3;
    font-weight: 600;
  }

  .tab-label-active {
    color: #ff8a65;
  }

  .tab-center-button {
    width: 56px;
    height: 56px;
    border-radius: 28px;
    background-color: #ff8a65;
    justify-content: center;
    align-items: center;
    box-shadow: 0 12px 24px rgba(255, 138, 101, 0.38);
  }
</style>