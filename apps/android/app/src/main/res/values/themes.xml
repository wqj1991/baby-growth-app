<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Baobao" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_500</item>
        <item name="colorPrimaryVariant">@color/blue_800</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorOnPrimary</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="fontFamily">sans-serif</item>
    </style>

    <!-- Alias to match manifest casing and avoid resource-not-found during linking. -->
    <style name="Theme.BaoBao" parent="Theme.Baobao" />

    <style name="Widget.App.Button.QuickLog" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="iconTint">@color/text_secondary</item>
        <item name="iconPadding">@dimen/spacing_4</item>
        <item name="strokeWidth">0dp</item>
        <item name="android:background">@drawable/rounded_card_background</item>
        <item name="android:padding">@dimen/spacing_4</item>
        <item name="rippleColor">@color/blue_50</item>
    </style>

    <style name="Widget.App.Button.SettingsItem" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="iconTint">@color/text_secondary</item>
        <item name="iconPadding">@dimen/spacing_4</item>
        <item name="android:padding">@dimen/spacing_4</item>
        <item name="rippleColor">@color/blue_50</item>
        <item name="iconGravity">start</item>
    </style>
</resources>
