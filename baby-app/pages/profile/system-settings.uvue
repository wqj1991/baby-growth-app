<template>
  <view class="page-root">
    <!-- Status Bar -->
    <view class="status-bar">
      <text class="status-time">10:52</text>
      <view class="status-icons">
        <view class="status-icon">
          <uni-icons type="signal" :size="15" color="#1f2937"></uni-icons>
        </view>
        <view class="status-icon">
          <uni-icons type="wifi" :size="15" color="#1f2937"></uni-icons>
        </view>
        <view class="status-icon">
          <uni-icons type="battery-full" :size="17" color="#1f2937"></uni-icons>
        </view>
      </view>
    </view>

    <!-- Main Content -->
    <scroll-view class="page-scroll" :show-scrollbar="false">
      <!-- Header -->
      <view class="header-slate">
        <view class="header-nav">
          <view class="nav-back" @tap="goBack">
            <uni-icons type="back" :size="20" color="#ffffff"></uni-icons>
          </view>
          <text class="nav-title">偏好设置</text>
          <text class="nav-action">保存</text>
        </view>
        <view class="header-content">
          <text class="header-title">系统设置</text>
          <text class="header-desc">管理通用体验、网络策略与隐私选项，不包含登录管理。</text>
        </view>
      </view>

      <!-- Content Section -->
      <view class="content-section">
        <!-- Interface & Interaction -->
        <view class="card">
          <text class="card-title">界面与交互</text>
          <view class="switch-list">
            <view class="switch-item">
              <text class="switch-label">跟随系统字体大小</text>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
            <view class="switch-item">
              <text class="switch-label">启用页面动效</text>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
            <view class="switch-item">
              <text class="switch-label">首页默认打开时间轴</text>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
          </view>
        </view>

        <!-- Network & Sync -->
        <view class="card">
          <text class="card-title">网络与同步</text>
          <view class="switch-list">
            <view class="switch-item">
              <text class="switch-label">仅 Wi-Fi 上传原视频</text>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
            <view class="switch-item">
              <text class="switch-label">弱网自动暂停同步</text>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
            <view class="switch-item">
              <text class="switch-label">流量下只加载缩略图</text>
              <uni-icons type="switch" :size="28" color="#d1d5db"></uni-icons>
            </view>
          </view>
        </view>

        <!-- Privacy & Security -->
        <view class="card">
          <text class="card-title">隐私与安全</text>
          <view class="switch-list">
            <view class="switch-item">
              <text class="switch-label">截图水印（家庭空间）</text>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
            <view class="switch-item">
              <text class="switch-label">健康记录二次确认删除</text>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
            <view class="switch-item action-arrow">
              <text class="switch-label">导出本地诊断日志</text>
              <uni-icons type="right" :size="16" color="#d1d5db"></uni-icons>
            </view>
          </view>
        </view>

        <!-- About -->
        <view class="card">
          <text class="card-title">关于</text>
          <view class="about-list">
            <view class="about-item">
              <text class="about-label">当前版本</text>
              <text class="about-value">v0.9.3</text>
            </view>
            <view class="about-item action-arrow" @tap="viewChangelog">
              <text class="about-label">更新日志</text>
              <uni-icons type="right" :size="16" color="#d1d5db"></uni-icons>
            </view>
          </view>
        </view>
      </view>

      <view class="bottom-space"></view>
    </scroll-view>

    <!-- Bottom Actions -->
    <view class="bottom-bar">
      <view class="btn btn-secondary" @tap="goBack">
        <text>返回我的</text>
      </view>
      <view class="btn btn-primary btn-wide" @tap="saveSettings">
        <text>保存系统设置</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
  function goBack() {
    uni.navigateBack()
  }

  function viewChangelog() {
    uni.showToast({
      title: '开发中',
      icon: 'none'
    })
  }

  function saveSettings() {
    uni.showToast({
      title: '保存成功',
      icon: 'success'
    })
    setTimeout(() => {
      uni.navigateBack()
    }, 1500)
  }
</script>

<style>
  .page-root {
    flex: 1;
    height: 100%;
    background-color: #f1f5f9;
  }

  .status-bar {
    height: 40px;
    background-color: rgba(255, 255, 255, 0.8);
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 0 16px;
    font-size: 14px;
    font-weight: 500;
    color: #1f2937;
  }

  .status-icons {
    display: flex;
    flex-direction: row;
  }

  .status-icon {
    margin-right: 8px;
    display: flex;
    flex-direction: row;
    align-items: center;
  }

  .page-scroll {
    flex: 1;
    height: 100%;
  }

  .header-slate {
    background: linear-gradient(135deg, #374151 0%, #4b5563 50%, #6b7280 100%);
    padding: 20px 20px 32px;
    border-radius: 0 0 32px 32px;
    box-shadow: 0 10px 25px rgba(55, 65, 81, 0.3);
  }

  .header-nav {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }

  .nav-back {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: rgba(255, 255, 255, 0.2);
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
  }

  .nav-title {
    font-size: 14px;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.9);
    letter-spacing: 1px;
  }

  .nav-action {
    font-size: 14px;
    font-weight: 600;
    color: #ffffff;
  }

  .header-content {
    margin-top: 24px;
  }

  .header-title {
    font-size: 32px;
    font-weight: 700;
    color: #ffffff;
    display: block;
  }

  .header-desc {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.8);
    margin-top: 8px;
    display: block;
  }

  .content-section {
    padding: 0 16px;
    margin-top: -20px;
  }

  .card {
    background-color: #ffffff;
    border-radius: 24px;
    padding: 16px;
    margin-bottom: 16px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  }

  .card-title {
    font-size: 14px;
    font-weight: 600;
    color: #111827;
    display: block;
  }

  .switch-list {
    margin-top: 12px;
  }

  .switch-item {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 12px;
    border-radius: 16px;
    background-color: #f8fafc;
    margin-bottom: 12px;
    font-size: 14px;
    color: #374151;
  }

  .switch-item:last-child {
    margin-bottom: 0;
  }

  .action-arrow {
    color: #6b7280;
  }

  .about-list {
    margin-top: 12px;
  }

  .about-item {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 12px;
    border-radius: 16px;
    background-color: #f8fafc;
    margin-bottom: 12px;
    font-size: 14px;
  }

  .about-item:last-child {
    margin-bottom: 0;
  }

  .about-label {
    color: #6b7280;
  }

  .about-value {
    font-weight: 600;
    color: #1f2937;
  }

  .bottom-space {
    height: 100px;
  }

  .bottom-bar {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 12px 16px;
    padding-bottom: calc(12px + env(safe-area-inset-bottom));
    background-color: #ffffff;
    border-top: 1px solid #e5e7eb;
    display: flex;
    gap: 12px;
  }

  .btn {
    flex: 1;
    padding: 14px;
    border-radius: 16px;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    font-size: 15px;
    font-weight: 600;
  }

  .btn-secondary {
    background-color: #f1f5f9;
    color: #4b5563;
  }

  .btn-primary {
    background-color: #374151;
    color: #ffffff;
    box-shadow: 0 4px 15px rgba(55, 65, 81, 0.3);
  }

  .btn-wide {
    flex: 1.4;
  }
</style>