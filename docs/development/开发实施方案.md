# 宝宝成长记录 APP 开发实施方案（Android + HarmonyOS）

---

## 1. 总览

### 1.1 项目目标
- 交付 Android 与 HarmonyOS 两套客户端，保持双端业务行为一致，避免规则漂移。
- 无自建服务器：**Phase 1** 以本地存储实现完整功能闭环；**Phase 2** 整体切换为夸克网盘同步与备份，业务代码零改动。

### 1.2 实施范围（对应 PRD）
- 启动授权、共享账号登录、家庭空间初始化。
- 影像链路：拍照/录像、本地上传、缓存预览、手动下载。
- 育儿记录：喂养/大小便/睡眠/健康记录。
- 同步链路（Phase 2）：进页面拉配置、下拉刷新、10 分钟自动同步、离线补传。
- 设置与安全：缓存配置、下载路径、日志与配置加密。

### 1.3 明确边界
- Phase 2 夸克网盘仅使用：文件上传 / 下载 / 目录管理。
- 冲突策略：Last Write Wins（最后写入覆盖）。
- 加密边界：仅日志与配置文件加密，媒体文件不加密。

### 1.4 代码组织（Monorepo）
```
apps/android       ← Android 客户端（Kotlin + Jetpack）
apps/harmony       ← HarmonyOS 客户端（ArkTS + Stage Model）
core/specs         ← 共享协议与规则（字段、命名、目录、版本）
docs/requirements  ← 需求文档
docs/ux            ← 高保真原型
```

### 1.5 共享与差异划分
- **共享规则**：数据模型、同步协议、冲突策略、缓存规则、加密规范。
- **平台实现**：权限系统、相机/相册、文件系统、后台任务、UI 渲染。

---

## 2. 架构设计

### 2.1 核心设计原则

| 原则 | 说明 |
|------|------|
| **存储无关** | 所有业务逻辑只依赖抽象接口，不直接访问文件系统或网盘 SDK |
| **目录镜像** | 本地目录结构与夸克网盘目录结构完全对齐，切换时无需重组数据 |
| **离线优先** | 写入永远先落本地，同步为后置动作（Phase 2 才有意义） |
| **加密边界一致** | 本地与云端采用同一套加密字段和算法，Phase 2 直接复用 |

### 2.2 存储抽象层（Storage Abstraction Layer）

#### 核心接口

```
IStorageProvider
├── IConfigStorage      // 应用配置 & 宝宝档案
├── ILogStorage         // 结构化育儿日志
├── IMediaStorage       // 媒体文件 & 元数据
└── ISyncQueueStorage   // 同步任务队列（Phase 1 仅本地持久化）
```

```kotlin
// Android 示例（Harmony ArkTS 结构对等）
interface IConfigStorage {
    suspend fun readConfig(): AppConfig
    suspend fun writeConfig(config: AppConfig)
    suspend fun readBabyProfile(): BabyProfile
    suspend fun writeBabyProfile(profile: BabyProfile)
}

interface ILogStorage {
    suspend fun insertLog(log: CareLog): String
    suspend fun queryLogs(filter: LogFilter): List<CareLog>
    suspend fun updateLog(log: CareLog)
    suspend fun deleteLog(logId: String)
}

interface IMediaStorage {
    suspend fun saveRawMedia(file: LocalFile): MediaItem
    suspend fun savePreview(mediaId: String, thumb: ByteArray)
    suspend fun queryMedia(filter: MediaFilter): List<MediaItem>
    suspend fun deleteMedia(mediaId: String)
    fun getCacheSizeBytes(): Long
    suspend fun clearPreviewCache(olderThanDays: Int = 7)
}

interface ISyncQueueStorage {
    suspend fun enqueue(task: SyncTask)
    suspend fun dequeue(limit: Int = 20): List<SyncTask>
    suspend fun markDone(taskId: String)
    suspend fun markFailed(taskId: String, reason: String)
}
```

#### Phase 1 实现 — LocalStorageProvider

```
LocalStorageProvider
├── LocalConfigStorage      → 加密 JSON 文件
├── LocalLogStorage         → SQLite（baobao.db，care_logs 表）
├── LocalMediaStorage       → 文件系统 + SQLite（media_items 表）
└── LocalSyncQueueStorage   → SQLite（sync_tasks 表，Phase 1 立即标记为 DONE）
```

#### Phase 2 实现 — QuarkStorageProvider

```
QuarkStorageProvider
├── QuarkConfigStorage      → 网盘 /Baby/Config/ 加密文件
├── QuarkLogStorage         → 网盘 /Baby/Logs/{year-month}/ 加密 JSON
├── QuarkMediaStorage       → 网盘 /Baby/Media/raw/ + /Baby/Media/preview/
└── QuarkSyncQueueStorage   → 本地 SQLite（队列本身永远在本地）
```

> Phase 2 只需在 DI 容器中将注入实现从 `Local*` 替换为 `Quark*`，业务层无感知。

### 2.3 本地目录结构

```
[AppPrivateDir]/              ← Android: filesDir  |  Harmony: getContext().filesDir
├── Baby/
│   ├── Config/
│   │   ├── app_config.enc    ← AppConfig（AES-256-GCM 加密）
│   │   └── baby_profile.enc  ← BabyProfile（AES-256-GCM 加密）
│   └── Logs/
│       ├── 2025-04/
│       │   └── logs.enc      ← 当月 CareLog 列表（AES-256-GCM 加密）
│       └── 2025-05/
│           └── logs.enc
│
├── db/
│   └── baobao.db             ← SQLite：care_logs / media_items / sync_tasks
│
├── media/
│   ├── raw/                  ← APP 专属相册真实文件（拍摄写入此处，注册媒体库）
│   │   ├── {mediaId}.jpg
│   │   └── {mediaId}.mp4
│   └── preview/              ← 预览缩略图缓存（不注册媒体库，可自动清理）
│       └── {mediaId}_thumb.jpg
│
└── cache/                    ← 其他临时缓存（Phase 2：网盘预览缓存落此处）
```

#### 本地与夸克目录映射关系

| 本地路径 | 夸克网盘路径 | 切换动作 |
|---------|------------|---------|
| `Baby/Config/app_config.enc` | `/Baby/Config/app_config.enc` | 直接上传，路径不变 |
| `Baby/Config/baby_profile.enc` | `/Baby/Config/baby_profile.enc` | 直接上传，路径不变 |
| `Baby/Logs/{month}/logs.enc` | `/Baby/Logs/{month}/logs.enc` | 直接上传，路径不变 |
| `media/raw/{mediaId}.*` | `/Baby/Media/raw/{mediaId}.*` | 上传原文件 |
| `media/preview/{mediaId}_thumb.*` | `/Baby/Media/preview/{mediaId}_thumb.*` | 可重新生成或上传 |

### 2.4 三类媒体管理规则

| 类别 | 来源 | 本地路径 | 注册媒体库 | 可清理 |
|------|------|---------|-----------|-------|
| **本地实拍** | APP 内置相机 | `media/raw/` | ✅ 专属相册 | ❌ 用户主动删除 |
| **云端缓存预览** | Phase 2 网盘缩略图 | `media/preview/` | ❌ | ✅ 自动 7 天 / 手动清空 |
| **手动下载** | 用户主动下载 | 用户选择路径 | ✅ 专属相册或系统相册 | ❌ 用户主动删除 |

---

## 3. 数据模型（core/specs 共享定义）

### 3.1 AppConfig

```json
{
  "version": 1,
  "cacheMaxBytes": 2147483648,
  "cacheAutoCleanDays": 7,
  "downloadPath": "APP_ALBUM",
  "wifiOnlyUpload": true,
  "syncIntervalMinutes": 10,
  "familyMembers": [
    { "id": "uuid", "nickname": "妈妈", "role": "parent" }
  ]
}
```

### 3.2 BabyProfile

```json
{
  "id": "uuid",
  "name": "小宝",
  "birthday": "2024-08-01",
  "gender": "male",
  "avatarMediaId": "uuid",
  "updatedAt": "2025-04-28T10:00:00Z",
  "updatedBy": "member-uuid"
}
```

### 3.3 CareLog

```json
{
  "id": "uuid",
  "type": "FEEDING | POTTY | SLEEP | TEMPERATURE | VACCINE | VISIT | MEDICATION | GROWTH | CUSTOM",
  "babyId": "uuid",
  "recordedAt": "2025-04-28T08:30:00Z",
  "recordedBy": "member-uuid",
  "data": {},
  "note": "备注",
  "mediaIds": [],
  "createdAt": "2025-04-28T08:31:00Z",
  "updatedAt": "2025-04-28T08:31:00Z",
  "deleted": false
}
```

**各类型 data 字段：**

| type | 关键字段 |
|------|---------|
| FEEDING | `feedingType(BREAST/FORMULA/SOLID)`, `amountMl`, `durationMin`, `side(L/R/BOTH)` |
| POTTY | `pottyType(PEE/POO/BOTH)`, `color`, `consistency` |
| SLEEP | `startAt`, `endAt`, `durationMin`, `quality` |
| TEMPERATURE | `valueCelsius`, `measureSite(ARMPIT/RECTAL/EAR)` |
| VACCINE | `vaccineName`, `dose`, `site`, `nextDueDate` |
| VISIT | `doctorName`, `hospital`, `diagnosis`, `nextVisitDate` |
| MEDICATION | `medicationName`, `dosage`, `unit`, `frequency` |
| GROWTH | `weightKg`, `heightCm`, `headCircumferenceCm` |

### 3.4 MediaItem

```json
{
  "id": "uuid",
  "type": "PHOTO | VIDEO",
  "source": "APP_SHOT | LOCAL_IMPORT | CLOUD_DOWNLOAD",
  "localRawPath": "/data/.../media/raw/uuid.jpg",
  "localPreviewPath": "/data/.../media/preview/uuid_thumb.jpg",
  "cloudRawPath": "/Baby/Media/raw/uuid.jpg",
  "fileSizeBytes": 3145728,
  "durationSeconds": null,
  "capturedAt": "2025-04-28T08:00:00Z",
  "uploadedAt": null,
  "babyId": "uuid",
  "takenBy": "member-uuid",
  "deleted": false
}
```

### 3.5 SyncTask

```json
{
  "id": "uuid",
  "action": "UPLOAD | DOWNLOAD | DELETE",
  "resourceType": "CONFIG | LOG | MEDIA",
  "resourceId": "uuid",
  "localPath": "...",
  "remotePath": "...",
  "status": "PENDING | IN_PROGRESS | DONE | FAILED",
  "retryCount": 0,
  "createdAt": "...",
  "updatedAt": "..."
}
```

> Phase 1：所有新增记录生成 SyncTask 后立即标记为 `DONE`（无实际上传）。  
> Phase 2：SyncEngine 消费队列，调用 QuarkStorageProvider 执行真实上传/下载。

### 3.6 SQLite 表结构

```sql
-- 育儿日志
CREATE TABLE care_logs (
    id TEXT PRIMARY KEY,
    type TEXT NOT NULL,
    baby_id TEXT NOT NULL,
    recorded_at INTEGER NOT NULL,     -- Unix 毫秒
    recorded_by TEXT NOT NULL,
    data TEXT NOT NULL,               -- JSON
    note TEXT,
    media_ids TEXT,                   -- JSON 数组
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL,
    deleted INTEGER NOT NULL DEFAULT 0
);
CREATE INDEX idx_care_logs_baby_time ON care_logs(baby_id, recorded_at DESC);
CREATE INDEX idx_care_logs_type ON care_logs(type, baby_id, recorded_at DESC);

-- 媒体元数据
CREATE TABLE media_items (
    id TEXT PRIMARY KEY,
    type TEXT NOT NULL,
    source TEXT NOT NULL,
    local_raw_path TEXT,
    local_preview_path TEXT,
    cloud_raw_path TEXT,
    file_size_bytes INTEGER,
    duration_seconds INTEGER,
    captured_at INTEGER NOT NULL,
    uploaded_at INTEGER,
    baby_id TEXT NOT NULL,
    taken_by TEXT NOT NULL,
    deleted INTEGER NOT NULL DEFAULT 0
);
CREATE INDEX idx_media_items_baby_time ON media_items(baby_id, captured_at DESC);

-- 同步任务队列
CREATE TABLE sync_tasks (
    id TEXT PRIMARY KEY,
    action TEXT NOT NULL,
    resource_type TEXT NOT NULL,
    resource_id TEXT NOT NULL,
    local_path TEXT,
    remote_path TEXT,
    status TEXT NOT NULL DEFAULT 'PENDING',
    retry_count INTEGER NOT NULL DEFAULT 0,
    error_message TEXT,
    created_at INTEGER NOT NULL,
    updated_at INTEGER NOT NULL
);
CREATE INDEX idx_sync_tasks_status ON sync_tasks(status, created_at ASC);
```

### 3.7 加密规范

| 项目 | 规格 |
|------|------|
| 算法 | AES-256-GCM |
| 密钥存储 | Android Keystore / HarmonyOS HUKS |
| 加密范围 | `app_config.enc`、`baby_profile.enc`、`logs.enc` |
| 不加密 | SQLite 文件本身（使用系统 AppPrivateDir 隔离）、媒体文件 |
| Phase 2 一致性 | 云端存储格式与本地加密格式完全相同，上传即可，无需重新加密 |

---

## 4. 模块设计

### 4.1 模块依赖关系

```
┌─────────────────────────────────────────────────────────┐
│                      UI Layer                           │
│  HomeFragment  RecordFragment  StatsFragment  MineTab   │
└──────────────────────┬──────────────────────────────────┘
                       │ 调用 ViewModel / UseCase
┌──────────────────────▼──────────────────────────────────┐
│                  Domain Layer                           │
│   CareLogUseCase  MediaUseCase  ConfigUseCase           │
│   SyncUseCase (Phase 1: no-op  | Phase 2: real sync)    │
└──────────────────────┬──────────────────────────────────┘
                       │ 依赖抽象接口
┌──────────────────────▼──────────────────────────────────┐
│              Storage Abstraction Layer                  │
│   IConfigStorage  ILogStorage  IMediaStorage            │
│   ISyncQueueStorage                                     │
└──────────┬──────────────────────────────┬───────────────┘
           │ Phase 1                      │ Phase 2
┌──────────▼───────────┐    ┌─────────────▼──────────────┐
│  LocalStorageProvider │    │  QuarkStorageProvider      │
│  (文件系统 + SQLite)   │    │  (夸克 SDK + 本地 SQLite)  │
└──────────────────────┘    └────────────────────────────┘
```

### 4.2 Auth 模块
- 共享账号登录态管理。
- 启动授权流程编排（相机/相册/存储/通知权限）。

### 4.3 Media 模块
- 内置拍摄：本地专属相册落盘，注册媒体库。
- 本地上传：仅写入元数据 + 入同步队列，不改动原图。
- 浏览：优先 `media/preview/` 缓存预览，禁止自动下载原文件。
- 下载：用户主动触发，默认保存专属相册，可改保存路径。

### 4.4 CareLog 模块
- 喂养、排泄、睡眠、健康全类型记录录入与查询。
- 本地暂存（SQLite），Phase 2 联网同步。

### 4.5 Sync 模块（Phase 2 生效）
- 触发器：进入页面、下拉刷新、10 分钟定时。
- 任务队列：待上传、待下载、失败重试（退避策略）。
- 断网恢复：自动补传与增量刷新。
- 冲突处理：Last Write Wins（`updatedAt` 比较）。

### 4.6 Settings 模块
- 缓存上限（默认 2 GB）、自动清理周期（默认 7 天）。
- 下载目录配置与手动清理动作。

### 4.7 Security 模块
- AES-256-GCM 加解密工具。
- 密钥安全存储（Android Keystore / HarmonyOS HUKS）与轮换策略预留。

---

## 5. 开发任务清单

### 5.1 P0 — MVP（Phase 1 本地存储闭环）

| # | 任务 |
|---|------|
| 1 | 工程初始化与模块骨架搭建（Android / HarmonyOS） |
| 2 | SQLite 建表 + LocalStorageProvider 完整实现 |
| 3 | 加密工具类（AES-256-GCM + Keystore/HUKS） |
| 4 | 启动授权 + 共享账号登录 + 家庭空间初始化 |
| 5 | ConfigStorage + BabyProfile 读写 |
| 6 | CareLog 全类型录入与查询（喂养/大小便/睡眠/健康） |
| 7 | 拍照/录像并保存专属相册 |
| 8 | 本地相册上传（原图不改动，仅写元数据） |
| 9 | 动态流缓存预览与手动下载 |
| 10 | 我的页：缓存配置、下载路径、手动清理缓存 |
| 11 | SyncTask 占位实现（写入后立即标记 DONE） |
| 12 | 双端一致性联调与回归 |

### 5.2 P0 — Phase 2 夸克切换

| # | 任务 |
|---|------|
| 1 | 引入夸克 SDK 依赖 |
| 2 | 实现 QuarkStorageProvider（4 个子类） |
| 3 | DI 容器替换绑定（`Local*` → `Quark*`） |
| 4 | 启用 SyncEngine（消费 sync_tasks 队列） |
| 5 | 数据迁移脚本（本地 `Baby/` → 网盘 `/Baby/`，路径不变） |
| 6 | 离线队列与自动补传 |
| 7 | 日志与配置加密上云验证 |
| 8 | 双端回归与 Beta 发布准备 |

### 5.3 P1 — 增强

1. 数据统计页图表化展示。
2. 同步性能和耗电优化（WiFi 优先、断点续传）。
3. 异常场景可观测性增强（埋点、错误分类）。

---

## 6. 里程碑计划

### Phase 1（第 1-6 周）

```
M1  工程骨架 + DI 容器 + 数据库初始化
    └─ SQLite 建表 + LocalStorageProvider 空实现

M2  ConfigStorage + BabyProfile 读写
    └─ 加密工具类（AES-GCM + Keystore/HUKS）

M3  CareLog 全类型录入 + 查询
    └─ LocalLogStorage 完整实现

M4  媒体链路（拍摄落盘 / 本地上传元数据 / 三类管理）
    └─ LocalMediaStorage 完整实现

M5  UI 接入（首页时间轴 / 快速记录 / 统计 / 我的）
    └─ ViewModel 层对接 UseCase

M6  缓存管理（容量统计 / 自动清理 / 手动清空）
    └─ Phase 1 完整闭环验收
```

### Phase 2（第 7-9 周）

```
M7  夸克 SDK 接入 + QuarkStorageProvider 实现
M8  SyncEngine + 离线补传 + 冲突处理
M9  数据迁移 + 双端回归 + Beta 发布准备
```

---

## 7. 验收标准（DoD）

### 7.1 功能验收
- 拍摄内容本地可见，Phase 2 可同步上云。
- 本地上传不删除、不改动原图。
- 浏览默认缓存预览，无自动原图下载。
- 用户手动下载可选路径，默认专属相册。
- Phase 2：进入页面/下拉/10 分钟同步策略生效。
- Phase 2：离线记录与拍摄恢复网络后可自动补传。

### 7.2 一致性验收
- Android 与 HarmonyOS 对同一数据集展示一致。
- 双端冲突结果一致遵循 LWW。
- 配置项默认值一致（2 GB、7 天）。

### 7.3 安全验收
- 日志与配置本地及云端均为密文。
- 本地读取与解密流程正常。
- 媒体文件本地明文，存储于 AppPrivateDir 系统隔离。

---

## 8. 风险与应对

| 风险 | 应对策略 |
|------|---------|
| Phase 2 夸克接口稳定性 | 增加重试、指数退避、本地队列保护 |
| 双端行为偏差 | 建立 `core/specs` 共享协议文档与双端回归清单 |
| 大文件同步耗时 | WiFi 优先上传 + 断点续传策略 |
| 权限被拒链路中断 | 降级提示 + 引导二次授权入口 |
| Phase 1→2 迁移数据丢失 | 迁移前全量本地备份，迁移后对比校验 |
