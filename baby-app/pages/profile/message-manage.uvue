<template>
  <view class="page-root">
    <!-- Status Bar -->
    <view class="status-bar">
      <text class="status-time">10:53</text>
      <view class="status-icons">
        <view class="status-icon">
          <uni-icons type="signal" :size="15" color="#1f2937"></uni-icons>
        </view>
        <view class="status-icon">
          <uni-icons type="wifi" :size="15" color="#1f2937"></uni-icons>
        </view>
        <view class="status-icon">
          <uni-icons type="battery-three-quarters" :size="17" color="#1f2937"></uni-icons>
        </view>
      </view>
    </view>

    <!-- Main Content -->
    <scroll-view class="page-scroll" :show-scrollbar="false">
      <!-- Header -->
      <view class="header-indigo">
        <view class="header-nav">
          <view class="nav-back" @tap="goBack">
            <uni-icons type="back" :size="20" color="#ffffff"></uni-icons>
          </view>
          <text class="nav-title">通知中心</text>
          <text class="nav-action">静默时段</text>
        </view>
        <view class="header-content">
          <text class="header-title">消息管理</text>
          <text class="header-desc">按类型配置推送强度，避免打扰，同时保留关键提醒。</text>
        </view>
      </view>

      <!-- Content Section -->
      <view class="content-section">
        <!-- Critical Alerts -->
        <view class="card">
          <text class="card-title">关键提醒</text>
          <view class="critical-list">
            <view class="critical-item">
              <view class="critical-info">
                <text class="critical-title">健康异常提醒</text>
                <text class="critical-desc">发热、用药冲突、持续症状</text>
              </view>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
            <view class="critical-item">
              <view class="critical-info">
                <text class="critical-title">同步失败告警</text>
                <text class="critical-desc">云端上传失败或冲突未解决</text>
              </view>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
          </view>
        </view>

        <!-- Daily Messages -->
        <view class="card">
          <text class="card-title">日常消息</text>
          <view class="switch-list">
            <view class="switch-item">
              <text class="switch-label">家庭评论与点赞</text>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
            <view class="switch-item">
              <text class="switch-label">喂养/睡眠建议提醒</text>
              <uni-icons type="switch" :size="28" color="#d1d5db"></uni-icons>
            </view>
            <view class="switch-item">
              <text class="switch-label">每周成长报告推送</text>
              <uni-icons type="switch" :size="28" color="#22c55e"></uni-icons>
            </view>
          </view>
        </view>

        <!-- Do Not Disturb -->
        <view class="card">
          <view class="card-header">
            <text class="card-title">免打扰时段</text>
            <text class="card-badge">已启用</text>
          </view>
          <view class="dnd-grid">
            <view class="dnd-item">
              <text class="dnd-label">开始时间</text>
              <text class="dnd-value">22:00</text>
            </view>
            <view class="dnd-item">
              <text class="dnd-label">结束时间</text>
              <text class="dnd-value">07:00</text>
            </view>
          </view>
          <text class="dnd-note">健康异常提醒不受免打扰影响。</text>
        </view>
      </view>

      <view class="bottom-space"></view>
    </scroll-view>

    <!-- Bottom Actions -->
    <view class="bottom-bar">
      <view class="btn btn-secondary" @tap="goBack">
        <text>返回我的</text>
      </view>
      <view class="btn btn-primary btn-wide" @tap="saveSettings">
        <text>保存消息配置</text>
      </view>
    </view>
  </view>
</template>

<script setup lang="uts">
  function goBack() {
    uni.navigateBack()
  }

  function saveSettings() {
    uni.showToast({
      title: '保存成功',
      icon: 'success'
    })
    setTimeout(() => {
      uni.navigateBack()
    }, 1500)
  }
</script>

<style>
  .page-root {
    flex: 1;
    height: 100%;
    background-color: #eef2ff;
  }

  .status-bar {
    height: 40px;
    background-color: rgba(255, 255, 255, 0.8);
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 0 16px;
    font-size: 14px;
    font-weight: 500;
    color: #1f2937;
  }

  .status-icons {
    display: flex;
    flex-direction: row;
  }

  .status-icon {
    margin-right: 8px;
    display: flex;
    flex-direction: row;
    align-items: center;
  }

  .page-scroll {
    flex: 1;
    height: 100%;
  }

  .header-indigo {
    background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 50%, #d946ef 100%);
    padding: 20px 20px 32px;
    border-radius: 0 0 32px 32px;
    box-shadow: 0 10px 25px rgba(99, 102, 241, 0.3);
  }

  .header-nav {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }

  .nav-back {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background-color: rgba(255, 255, 255, 0.2);
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
  }

  .nav-title {
    font-size: 14px;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.9);
    letter-spacing: 1px;
  }

  .nav-action {
    font-size: 14px;
    font-weight: 600;
    color: #ffffff;
  }

  .header-content {
    margin-top: 24px;
  }

  .header-title {
    font-size: 32px;
    font-weight: 700;
    color: #ffffff;
    display: block;
  }

  .header-desc {
    font-size: 14px;
    color: rgba(255, 255, 255, 0.85);
    margin-top: 8px;
    display: block;
  }

  .content-section {
    padding: 0 16px;
    margin-top: -20px;
  }

  .card {
    background-color: #ffffff;
    border-radius: 24px;
    padding: 16px;
    margin-bottom: 16px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  }

  .card-title {
    font-size: 14px;
    font-weight: 600;
    color: #111827;
    display: block;
  }

  .card-header {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }

  .card-badge {
    font-size: 12px;
    font-weight: 600;
    color: #6366f1;
  }

  .critical-list {
    margin-top: 12px;
  }

  .critical-item {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 12px;
    border-radius: 16px;
    background-color: #eef2ff;
    border: 1px solid #ddd6fe;
    margin-bottom: 12px;
  }

  .critical-item:last-child {
    margin-bottom: 0;
  }

  .critical-info {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .critical-title {
    font-size: 14px;
    font-weight: 600;
    color: #111827;
  }

  .critical-desc {
    font-size: 12px;
    color: #6b7280;
    margin-top: 2px;
  }

  .switch-list {
    margin-top: 12px;
  }

  .switch-item {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    padding: 12px;
    border-radius: 16px;
    background-color: #f8fafc;
    margin-bottom: 12px;
    font-size: 14px;
    color: #374151;
  }

  .switch-item:last-child {
    margin-bottom: 0;
  }

  .dnd-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-top: 12px;
  }

  .dnd-item {
    background-color: #f8fafc;
    border-radius: 16px;
    padding: 12px;
    text-align: center;
  }

  .dnd-label {
    font-size: 12px;
    color: #9ca3af;
    display: block;
  }

  .dnd-value {
    font-size: 18px;
    font-weight: 700;
    color: #1f2937;
    margin-top: 4px;
    display: block;
  }

  .dnd-note {
    font-size: 12px;
    color: #9ca3af;
    margin-top: 12px;
    display: block;
  }

  .bottom-space {
    height: 100px;
  }

  .bottom-bar {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 12px 16px;
    padding-bottom: calc(12px + env(safe-area-inset-bottom));
    background-color: #ffffff;
    border-top: 1px solid #e5e7eb;
    display: flex;
    gap: 12px;
  }

  .btn {
    flex: 1;
    padding: 14px;
    border-radius: 16px;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    font-size: 15px;
    font-weight: 600;
  }

  .btn-secondary {
    background-color: #f1f5f9;
    color: #4b5563;
  }

  .btn-primary {
    background-color: #6366f1;
    color: #ffffff;
    box-shadow: 0 4px 15px rgba(99, 102, 241, 0.3);
  }

  .btn-wide {
    flex: 1.4;
  }
</style>